name: RetroPack CI / Verification Pipeline

on:
  push:
    branches: [ main, master, "feat/**", "fix/**", "v**", "v*" ]
  pull_request:
    branches: [ main, master, "v**", "v*" ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-and-verify:
    name: Run Multi-Module Unit Tests & Verification
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run Multi-Module Test Suite
        run: ./gradlew :core:test :runtime:retropack-runtime-mgba:test :template-apk:test :app:test --continue --stacktrace

      - name: Upload Test Results & Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gradle-test-reports
          path: |
            core/build/reports/tests/
            runtime/retropack-runtime-mgba/build/reports/tests/
            template-apk/build/reports/tests/
            app/build/reports/tests/
            build/reports/
          if-no-files-found: ignore
