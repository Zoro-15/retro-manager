name: Build Garnacha Boy for Android

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Ninja
        uses: seanmiddleditch/gha-setup-ninja@master

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install native Android toolchain
        run: sdkmanager "cmake;3.18.1" "ndk;22.1.7171670"

      - name: Test mGBA core lifecycle on host
        run: |
          cmake -S android/smoke -B build/mgba-smoke -G Ninja -DCMAKE_BUILD_TYPE=Release
          cmake --build build/mgba-smoke
          ctest --test-dir build/mgba-smoke --output-on-failure

      - name: Build mGBA app and core
        run: android/gradlew -p android lintDebug :app:testDebugUnitTest :app:assembleBenchmark :core:assembleBenchmark

      - name: Upload optimized APK
        uses: actions/upload-artifact@v4
        with:
          name: garnacha-boy-android-benchmark
          path: android/app/build/outputs/apk/benchmark/app-benchmark.apk

      - name: Upload optimized mGBA core AAR
        uses: actions/upload-artifact@v4
        with:
          name: garnacha-boy-android-core-benchmark
          path: android/core/build/outputs/aar/core-benchmark.aar
