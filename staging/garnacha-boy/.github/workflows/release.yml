name: Release

# Builds, signs, and publishes a Garnacha Boy release APK when a v* tag is
# pushed. The signing key never lives in the repo: it is restored from a base64
# secret into a runner-temp file, referenced by a keystore.properties that this
# job writes at build time (the same file build.gradle reads locally), then both
# are shredded. The release is published as a DRAFT prerelease so a bad build
# can never auto-publish — a human reviews and un-drafts it.
#
# Required repository secrets:
#   GARNACHA_KEYSTORE_BASE64    base64 of garnacha-release.jks (base64 -w0)
#   GARNACHA_KEYSTORE_PASSWORD  keystore password
#   GARNACHA_KEY_ALIAS          key alias (garnacha)
#   GARNACHA_KEY_PASSWORD       key password

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Ninja
        # pinned to a commit SHA: this job handles signing secrets, so a
        # mutable @master ref would be a supply-chain foothold on the runner.
        uses: seanmiddleditch/gha-setup-ninja@7e868db0f3406270dd46e1dac26c65f621456723 # master @ 2026-07-14

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407 # v3

      - name: Install the pinned native toolchain
        run: sdkmanager "cmake;3.18.1" "ndk;22.1.7171670"

      - name: Restore the signing key and credentials
        env:
          KEYSTORE_BASE64: ${{ secrets.GARNACHA_KEYSTORE_BASE64 }}
          STORE_PASSWORD: ${{ secrets.GARNACHA_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.GARNACHA_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.GARNACHA_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          for name in KEYSTORE_BASE64 STORE_PASSWORD KEY_ALIAS KEY_PASSWORD; do
            if [ -z "${!name}" ]; then
              echo "::error::GARNACHA_${name} secret is not set — cannot sign a release."
              exit 1
            fi
          done
          echo "${KEYSTORE_BASE64}" | base64 -d > "${RUNNER_TEMP}/garnacha-release.jks"
          test -s "${RUNNER_TEMP}/garnacha-release.jks"
          # build.gradle reads android/app/keystore.properties (gitignored);
          # write the CI copy pointing at the restored key.
          {
            echo "storeFile=${RUNNER_TEMP}/garnacha-release.jks"
            echo "storePassword=${STORE_PASSWORD}"
            echo "keyAlias=${KEY_ALIAS}"
            echo "keyPassword=${KEY_PASSWORD}"
          } > android/app/keystore.properties

      - name: Build the signed release APK
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          GARNACHA_VERSION_NAME="${VERSION}" \
          GARNACHA_VERSION_CODE="${GITHUB_RUN_NUMBER}" \
          android/gradlew -p android \
            lintDebug :app:testDebugUnitTest :app:assembleRelease

      - name: Verify the APK is signed with the expected key
        env:
          KEY_ALIAS: ${{ secrets.GARNACHA_KEY_ALIAS }}
          STORE_PASSWORD: ${{ secrets.GARNACHA_KEYSTORE_PASSWORD }}
        run: |
          set -euo pipefail
          APK=android/app/build/outputs/apk/release/app-release.apk
          if [ ! -f "${APK}" ]; then
            echo "::error::release APK missing — the build did not produce a signed, release-key artifact."
            exit 1
          fi
          APKSIGNER="${ANDROID_HOME}/build-tools/35.0.0/apksigner"
          # Confirm the APK carries a valid signature at all.
          "${APKSIGNER}" verify "${APK}"
          # Then confirm it is OUR key, not merely some valid key: compare the
          # APK's signing-cert SHA-256 against the restored keystore's cert.
          apk_fp=$("${APKSIGNER}" verify --print-certs "${APK}" \
            | grep -i "SHA-256 digest" | head -1 | grep -oiE "[0-9a-f]{64}")
          key_fp=$(keytool -list -v \
            -keystore "${RUNNER_TEMP}/garnacha-release.jks" \
            -alias "${KEY_ALIAS}" -storepass "${STORE_PASSWORD}" 2>/dev/null \
            | grep -i "SHA256:" | head -1 \
            | grep -oiE "([0-9a-f]{2}:){31}[0-9a-f]{2}" \
            | tr -d ":" | tr "A-F" "a-f")
          if [ -z "${apk_fp}" ] || [ "${apk_fp}" != "${key_fp}" ]; then
            echo "::error::APK signing cert does not match the Garnacha release key."
            exit 1
          fi
          echo "Signing cert verified against the Garnacha release key."
          mv "${APK}" "garnacha-boy-${GITHUB_REF_NAME}.apk"

      - name: Package corresponding source
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          set -euo pipefail
          ROOT="garnacha-boy-${GITHUB_REF_NAME}"
          git archive --format=tar --prefix="${ROOT}/" HEAD > source.tar
          git -C vendor/mgba archive --format=tar \
            --prefix="${ROOT}/vendor/mgba/" HEAD > mgba.tar
          tar --concatenate --file=source.tar mgba.tar
          gzip -9n source.tar
          mv source.tar.gz "${ROOT}-source.tar.gz"
          rm mgba.tar

      - name: Shred the signing material
        if: always()
        run: |
          shred -u "${RUNNER_TEMP}/garnacha-release.jks" 2>/dev/null || true
          shred -u android/app/keystore.properties 2>/dev/null || true

      - name: Publish the draft release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          files: |
            garnacha-boy-*.apk
            garnacha-boy-*-source.tar.gz
          body_path: CHANGELOG.md
          draft: true
          prerelease: true
