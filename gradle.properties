org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# NOTE: Do not set org.gradle.java.home here — it breaks portable/CI builds.
# JDK 17+ is resolved via JAVA_HOME / Gradle toolchain. See README quickstart.
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
